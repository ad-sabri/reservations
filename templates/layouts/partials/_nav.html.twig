<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js" integrity="sha384-OgVRvuATP1z7JjHLkuOU7Xw704+h835Lr+6QL9UvYjZE3Ipu6Tp75j7Bh/kR0JKI" crossorigin="anonymous"></script>

<nav class="navbar navbar-expand-lg" style="background-color: #e3f2fd;">
  <a class="navbar-brand" href="{{ path('home') }}"><img src="../images/test.png" alt="logo" height="50"></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="{{ path('home') }}"> Réservations <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('show') }}">Catalogue</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('artist') }}">Artistes</a>
      </li>
    </ul>
    <!-- ici le formulaire de connexion -->

    {% if app.user%}
        <a class="nav-link" href="{{ path('logout') }}"> 
        <button type="button" class="btn btn-outline-danger">Déconnexion</button>
        <span class="sr-only">(current)</span></a>
        <p> <span class="badge badge-secondary">{{ app.user.firstname }} {{ app.user.lastname }}</span></p>
    {% else %}
          <a class="nav-link" href="{{ path('login') }}"> 
        <button type="button" class="btn btn-outline-success">Connexion</button>
    <span class="sr-only">(current)</span></a>
    <a class="nav-link" href="{{ path('register') }}"> 
        <button type="button" class="btn btn-outline-primary">Inscription</button>
    <span class="sr-only">(current)</span></a>   
    {% endif %}
  </div>
</nav>


